# run this as 'source setup.sh' instead of './setup.sh' since ./ runs its own shell instance then closes, so the export changes aren't kept
export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk1.7.0_67.jdk/Contents/Home
